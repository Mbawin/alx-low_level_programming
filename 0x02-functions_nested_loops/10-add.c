#include "main.h"

/**
 * add - adds two numbers and returns the result
 * @g: number to be added to m
 * @m: number to be added to g
 * Return: sum of g and m
 */
int add(int g, int m)
{
	int w;

	w = g + m;

	return (w);
}
